[tool.poetry]
name = "google-cloud-automl"
version = "0.1.0"
description = ""
packages = [
    { include = "google_cloud_automl" },
    { include = "foobar", from ="google_cloud_automl_extra" },
    { include = "extra_glob/**/*.py" },
]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
