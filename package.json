{
  "name": "release-please",
  "version": "1.0.0",
  "description": "generate release PRs based on the conventionalcommits.org spec",
  "main": "./build/src/index.js",
  "bin": "./build/src/bin/release-please.js",
  "scripts": {
    "test": "cross-env ENVIRONMENT=test c8 --reporter=html --reporter=text mocha --recursive --timeout=5000 build/test",
    "system-test": "cross-env ENVIRONMENT=test mocha --timeout=5000 build/system-test",
    "test:all": "cross-env ENVIRONMENT=test c8 --reporter=html --reporter=text mocha --recursive --timeout=20000 build/system-test build/test",
    "test:snap": "SNAPSHOT_UPDATE=1 npm test",
    "clean": "gts clean",
    "prepare": "npm run compile",
    "lint": "gts check",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "pretest": "npm run compile"
  },
  "files": [
    "build/src",
    "!build/src/**/*.map"
  ],
  "repository": "googleapis/release-please",
  "keywords": [
    "release",
    "conventional-commits"
  ],
  "author": "Google Inc.",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/bcoe/release-please/issues"
  },
  "homepage": "https://github.com/bcoe/release-please#readme",
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.6",
    "@types/semver": "^6.0.0",
    "c8": "^4.0.0-candidate.0",
    "chai": "^4.2.0",
    "cross-env": "^5.2.0",
    "gts": "^0.9.0",
    "mocha": "^6.1.4",
    "nock": "^10.0.6",
    "snap-shot-it": "^6.3.3",
    "typescript": "^3.4.4"
  },
  "dependencies": {
    "@octokit/rest": "^16.24.1",
    "chalk": "^2.4.2",
    "concat-stream": "^2.0.0",
    "conventional-changelog-conventionalcommits": "^2.0.0",
    "conventional-changelog-writer": "^4.0.3",
    "conventional-commits-filter": "^2.0.1",
    "conventional-commits-parser": "^3.0.1",
    "figures": "^3.0.0",
    "parse-github-repo-url": "^1.4.1",
    "semver": "^6.0.0",
    "yargs": "^13.2.2"
  }
}
